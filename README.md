# Golang Android action

This action can build a golang Android binary.

## Inputs

## `args`

**Required** Arguments to provide to `go build`.

## Example usage

```yaml
uses: jandedobbeleer/golang-android-action@v1
with:
  args: '-o hello_world'
```
